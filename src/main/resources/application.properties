spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

spring.web.locale=pl_PL
spring.web.locale-resolver=fixed
spring.messages.encoding=UTF-8
spring.messages.basename=messages, messages_pl, ValidationMessages, ValidationMessages_pl
#spring.messages.fallback-to-system-locale=false
#spring.thymeleaf.cache=false

#spring.thymeleaf.mode=HTML
#server.error.include-message=always
#server.error.include-binding-errors=always

# Debug
# logging.level.org.springframework.security=DEBUG
# logging.level.org.thymeleaf=DEBUG
# spring.jpa.show-sql=true


# File upload
spring.servlet.multipart.max-file-size = 10MB
spring.servlet.multipart.max-request-size = 10MB
image.storage.location=/home/mariusz/app-image
item.default.image=missingPhoto.png

# Email configuration
spring.mail.host=${MAIL_HOST}
spring.mail.port=${MAIL_PORT}
spring.mail.username=${MAIL_USER}
spring.mail.password=${MAIL_PASSWORD}
spring.mail.from.address=${MAIL_FROM}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.ssl.enable=true
spring.mail.properties.mail.debug=false